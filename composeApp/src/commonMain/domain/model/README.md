# Carpeta Model

**Objetivo:** Contiene los modelos de datos del dominio. Estas son las clases que representan los objetos de negocio de la aplicación.

**Ejemplo:**
```kotlin
data class User(
    val id: String,
    val name: String,
    val email: String
)
```
